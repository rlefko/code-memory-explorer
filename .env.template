# Claude Code Memory Explorer - Environment Configuration Template
# Copy this file to .env and customize the values as needed

# Port Configuration
# These ports can be customized if the defaults are in use
NGINX_PORT=8080          # Main application entry point
BACKEND_PORT=8000        # Backend API service port
QDRANT_PORT=6333         # Qdrant vector database port
VITE_PORT=3000          # Frontend development server port (dev mode only)

# Backend Configuration
# For development mode when running frontend with vite dev server
VITE_BACKEND_URL=http://localhost:8000

# Default Collection Name (optional)
# COLLECTION=my-project

# Qdrant Configuration (optional)
# QDRANT_API_KEY=your-api-key-here

# Docker Platform (optional, auto-detected)
# Set to 'linux/amd64' for Intel/AMD or 'linux/arm64' for ARM/Apple Silicon
# DOCKER_PLATFORM=linux/arm64