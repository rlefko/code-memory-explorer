# Claude Code Memory Explorer - Environment Configuration Template
# Copy this file to .env and customize the values as needed

# ===================================
# Qdrant Configuration Mode
# ===================================
# Set to 'true' to use an external Qdrant instance (your existing Qdrant at localhost:6333)
# Set to 'false' (default) to spin up a new Qdrant container
USE_EXTERNAL_QDRANT=false

# External Qdrant Configuration (when USE_EXTERNAL_QDRANT=true)
# For macOS/Windows Docker Desktop: Use host.docker.internal
# For Linux: Use host IP address or 172.17.0.1
EXTERNAL_QDRANT_URL=http://host.docker.internal:6333

# Internal Qdrant Configuration (when USE_EXTERNAL_QDRANT=false)
# This will be used by the backend when using the internal Qdrant container
# Leave this as-is for internal mode
QDRANT_URL=http://qdrant:6333

# ===================================
# Port Configuration
# ===================================
# These ports can be customized if the defaults are in use
NGINX_PORT=8080          # Main application entry point
BACKEND_PORT=8000        # Backend API service port (internal)
QDRANT_PORT=6333         # Qdrant vector database port (only used in internal mode)
VITE_PORT=3000          # Frontend development server port (dev mode only)

# ===================================
# Backend Configuration
# ===================================
# For development mode when running frontend with vite dev server
VITE_BACKEND_URL=http://localhost:8000

# ===================================
# Default Collection Name (optional)
# ===================================
# Specify a default collection to load on startup
# COLLECTION=my-project

# ===================================
# Qdrant API Configuration (optional)
# ===================================
# If your external Qdrant requires authentication
# QDRANT_API_KEY=your-api-key-here

# ===================================
# Docker Platform (optional)
# ===================================
# Set to 'linux/amd64' for Intel/AMD or 'linux/arm64' for ARM/Apple Silicon
# Usually auto-detected, only set if you have issues
# DOCKER_PLATFORM=linux/arm64

# ===================================
# Quick Start Examples
# ===================================
# To use your existing Qdrant at localhost:6333:
#   1. Set USE_EXTERNAL_QDRANT=true
#   2. Run: make external-qdrant
#
# To use a fresh Qdrant container:
#   1. Set USE_EXTERNAL_QDRANT=false (default)
#   2. Run: make run